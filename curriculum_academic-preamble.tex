% Fix command clashes between standard LaTeX/Quarto defaults and newtxmath
\usepackage{amsmath}

\let\arrowvert\undefined
\let\arrowvert\relax
\let\Arrowvert\undefined
\let\Arrowvert\relax
\let\bracevert\undefined
\let\bracevert\relax

\usepackage{newtxtext}
\usepackage{newtxmath}
\usepackage{helvet}

% ---------- Colors, graphics, tables ----------
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{colortbl}
\usepackage{array}
\usepackage{enumitem}
\usepackage{needspace}

% ---------- Header / Footer ----------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\fancyhead[L]{\footnotesize Simone Romiti -- Curriculum Vitae}
%\fancyhead[R]{\footnotesize\today}
%\fancyfoot[C]{\footnotesize Page \thepage}

% ---------- Helper commands ----------
\newcommand{\cvhead}[1]{\vspace{8pt}\noindent\textbf{#1}\par\vspace{2pt}}
\newcommand{\cvsub}[1]{\vspace{4pt}\noindent\textit{#1}\par\vspace{1pt}}

\newenvironment{tightitemize}{\begin{itemize}[leftmargin=1.4em,itemsep=1pt,topsep=2pt,parsep=0pt]}{\end{itemize}}

\newenvironment{cvlist}[1][4.3cm]{%
  \begin{list}{}{%
    \setlength{\leftmargin}{\dimexpr#1+1.1cm\relax}%
    \setlength{\labelwidth}{#1}%
    \setlength{\labelsep}{0.5em}%
    \setlength{\itemindent}{0pt}%
    \setlength{\listparindent}{0pt}%
    \setlength{\itemsep}{5pt}%
    \setlength{\parsep}{0pt}%
    \setlength{\topsep}{3pt}%
    \renewcommand{\makelabel}[1]{##1\hfill}}}%
  {\end{list}}

\setlength{\parskip}{2pt}
\renewcommand{\arraystretch}{1.25}